1. 선언
- 리스트는 [], 튜플은 ()
- 리스트는 값 생성, 삭제, 수정이 가능하지만, 튜플은 값을 바꿀 수 없다.
- 1개의 요소를 가질때는 뒤에 반드시 ,를 붙여야한다.
- ()괄호를 생략해도 무방하다.
ex) t1 = () , t2 = (1,), t3 = (1,3), t4 = 1,2,3

2. 변경, 삭제
- 튜플은 변경이나 삭제가 불가능하다.
- del 함수나 해당 인덱스 값 변경이 불가능하다.

3. 다루기
- 변경이나 삭제가 불가능하다는 것만 제외하며 리스트와 동일

* 인덱싱, 슬라이싱, 더하기, 곱하기, 길이 구하기, 등등.. 리스트와 동일
